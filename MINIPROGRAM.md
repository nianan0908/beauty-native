# 顾客端微信小程序接入

当前实现采用“React 顾客端 + 微信小程序 `web-view` 外壳”。这样可以直接复用现有顾客端，不需要用原生小程序重写页面。

## 已包含的内容

- `miniprogram/`：微信小程序源码。
- `project.config.json`：微信开发者工具项目配置。
- `miniprogram/config.js`：唯一需要修改的顾客端网址配置。
- 未配置、网络失败和业务域名错误提示页。
- 小程序会直接进入顾客首页，不显示后台登录页。
- 小程序转发和朋友圈分享入口。
- iPhone 底部安全区适配。

## 1. 部署 Web 项目

安装依赖并生成静态文件：

```bash
pnpm install
pnpm build
```

将 `dist/` 部署到支持 HTTPS 的静态托管服务。必须部署在域名根路径，或者确保 Vite 的 `base` 与实际子路径一致。

部署完成后，先用手机浏览器确认以下地址可以访问：

```text
https://你的域名/?shop=qiguang&page=首页
```

可选托管平台包括 Vercel、Cloudflare Pages、腾讯云静态网站托管、阿里云 OSS 或自己的 Nginx。

## 2. 配置小程序顾客端地址

编辑 `miniprogram/config.js`：

```js
const CUSTOMER_H5_BASE_URL = "https://你的域名/";
```

不要把查询参数写入这里。小程序会自动追加：

```text
shop=qiguang&page=首页&source=wechat-mini-program
```

## 3. 导入微信开发者工具

1. 打开微信开发者工具。
2. 选择“导入项目”。
3. 项目目录选择本仓库根目录，而不是只选择 `miniprogram/`。
4. 将测试 AppID 替换成你自己的小程序 AppID。
5. 开发工具会根据 `project.config.json` 自动识别 `miniprogram/` 为小程序根目录。

仓库中的 `touristappid` 只用于查看项目结构。真机预览、业务域名和发布需要你自己的 AppID。

## 4. 配置微信业务域名

在微信公众平台的小程序后台，将 Web 顾客端域名添加为“业务域名”。通常需要：

- 使用已经备案的 HTTPS 域名。
- 下载微信提供的域名校验文件，并将其放到网站根目录。
- 确保校验文件可通过 `https://你的域名/校验文件名.txt` 直接访问。
- 重新构建和部署后，再在微信后台完成校验。

本地开发时，微信开发者工具可以暂时关闭域名校验，但真机预览和正式发布仍需按微信平台要求配置。`web-view` 是否可用还取决于当前小程序主体类型和微信平台规则。

## 5. 真机验证

建议按以下顺序检查：

1. 小程序打开后直接显示“栖光美学”会员首页。
2. 首页、预约、权益、我的四个导航可以切换。
3. 预约详情抽屉、订单详情和售后详情正常显示。
4. iPhone 底部导航没有被 Home Indicator 遮挡。
5. 退出小程序再进入，本机的会员演示数据仍然存在。

## 数据边界

当前仍是纯前端演示，业务数据保存在 WebView 自己的 `localStorage` 中：

- 同一部手机再次进入通常可以看到之前的数据。
- 手机和电脑不会共享预约及订单。
- 不同手机之间也不会共享数据。

如果需要演示“手机预约 → 电脑前台确认 → 手机收到消息”，需要再接入一个轻量共享数据服务；小程序外壳本身不解决跨设备同步。

## 常见问题

### 页面显示“配置顾客端地址”

`miniprogram/config.js` 仍然使用示例域名，请替换成实际 HTTPS 地址。

### 开发工具能打开，手机打不开

优先检查业务域名、HTTPS 证书、域名备案和小程序主体的 `web-view` 权限。

### H5 能打开，小程序内显示加载失败

确认配置的是“业务域名”，而不是只配置 request 合法域名；同时检查网址是否发生了跳转到另一个未配置域名。
